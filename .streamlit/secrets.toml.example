# Copy values into Streamlit Cloud → App settings → Secrets (TOML format)
# Backend must be deployed first (see DEPLOY.md) — use your Render/Railway URL

BACKEND_URL = "https://your-neural-forge-api.onrender.com"
